<h1>{{title}}</h1>

<div>
  {{{detailHTML}}} <!-- vehicle info -->
</div>

<h2>Customer Reviews</h2>

{{#if reviews.length}}
  <ul>
    {{#each reviews}}
      <li>
        <strong>{{review_title}}</strong> — Rating: {{review_rating}}/5  
        <p>{{review_text}}</p>
        <em>{{review_date}}</em>
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>No reviews yet.</p>
{{/if}}

<a class="add-review-btn" href="/review/add/{{vehicle.inv_id}}">Add a Review</a>
